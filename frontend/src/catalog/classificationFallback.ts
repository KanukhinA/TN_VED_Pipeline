/** Обязательный класс-заглушка в UI (всегда есть в перечне классов, не удаляется). */
export const CLASSIFICATION_FALLBACK_CLASS_ID = "прочее";

/** Низкий приоритет по умолчанию, чтобы срабатывал после правил с условиями. */
export const CLASSIFICATION_FALLBACK_PRIORITY = 10000;
